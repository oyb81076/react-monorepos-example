{
  "name": "react-mongorepos-example",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "jest",
    "build": "gulp",
    "clean": "rimraf packages/*/{lib,build,dist,*.log}",
    "start": "cd packages/admin-packager; yarn start"
  },
  "workspaces": [
    "packages/*",
    "scripts"
  ]
}
