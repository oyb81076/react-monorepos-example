{
  "name": "react-mongorepos-example",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "jest",
    "build": "tsc --project tsconfig.prod.json"
  },
  "workspaces": [
    "packages/*"
  ]
}
